return {
    settings = {
        ['rust-analyzer'] = {
            -- cargo = {
            --     features = { "all" },
            --     extraArgs = { "--all-features" },  -- Pass the all-features flag to cargo
            -- },
        }
    }
}
